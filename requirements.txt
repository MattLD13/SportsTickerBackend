# Web server
flask
flask-cors
waitress          # production WSGI server (fixed thread pool + idle-connection reaping)

# HTTP
requests
websocket-client
signalrcore

# Environment / config
python-dotenv

# Airport code lookup (flight tracker)
airportsdata

# Spotify integration
spotipy

# AI / Gemini (golf caddie commentary)
google-genai

# Logo image processing
Pillow
